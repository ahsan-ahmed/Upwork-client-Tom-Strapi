'use strict';

/**
 * pricing-support-section service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::pricing-support-section.pricing-support-section');
